# Version

**Player-facing (Unity `ProjectSettings` bundle version, `Application.version`):** 1.0 — v1.0 product release.

**Internal / repo build label:** 1.0.0.6 — Credits scroll: slightly larger dialog padding and ScrollView / vertical-scroller right margins (scrollbar nudged inward). Player-facing bundle version is still **1.0**.
