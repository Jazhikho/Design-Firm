# Version

**Player-facing (Unity `ProjectSettings` bundle version, `Application.version`):** 1.0 — v1.0 product release.

**Internal / repo build label:** 1.0.0.1 — Wardrobe covers-bottom UI and equip SFX: correct red border color (0–1), safe default border capture, null guards, `AudioManager` SFX key constants, `WardrobeState.GetCurrentItem` doc and default case. Player-facing bundle version is still **1.0**.
