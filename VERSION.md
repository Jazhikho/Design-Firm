# Version

**Player-facing (Unity `ProjectSettings` bundle version, `Application.version`):** 1.0 — v1.0 product release.

**Internal / repo build label:** 1.0.0.7 — Credits scroll: further inward nudge (dialog padding, ScrollView and vertical-scroller margins). Player-facing bundle version is still **1.0**.
