# Version

**Player-facing (Unity `ProjectSettings` bundle version, `Application.version`):** 0.2 — intentionally separate from the internal label below.

**Internal / repo build label:** 0.0.5.2 — Merge PR #54 (wardrobe dressing-room JPG), PR #55 (results background + ideal outfit preview; reconciled Addressables GUID with PR #54 and hardened `ResultsController`), and PR #56 (wardrobe scenario label uses scenario `name` instead of `description`). Post-merge: skip null entries when iterating `scoredItems` in `ScoreItem`. Player-facing bundle version unchanged at 0.2.
